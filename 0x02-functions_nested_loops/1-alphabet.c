#include "main.h"

/**
 * main - print the alphabet, in lowercase
 * Return: Always (sucess)
 */

int main(void)
{
	int c;

	for (c = 'a'; c <= 'z'; c++)
		_putchar(c);
	_putchar('\n');

	return (0);
}
